import makeStyles from '@material-ui/core/styles/makeStyles';

export const useStyles = makeStyles(() => ({
  root: {
    padding: 2,
    minWidth: 'auto',
  },
  outlined: {
    fontWeight: 'normal',
    textTransform: 'none',
  },
}));
