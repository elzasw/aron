spring:
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: false
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:testdb;DATABASE_TO_UPPER=false
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console

config:
  file: src/test/resources/config/types.yaml

aron:
  workDir: src/test/resources/

dao:
  dir:
    path: C:/temp/transfagent/daos/

# Parametry přístupu k jádru
aronCore:
  disabled: true
  ft:
    url: http://localhost:8089/ws/ft
    user: x
    pass: x
    soapLogging: false
  core:
    url: http://localhost:8089/ws/ft
    user: x
    pass: x
    soapLogging: false

elza:
  url: http://localhost:8080/services/ExportService
  username: Whois-import
  password: Whois-import
  soapLogging: false
