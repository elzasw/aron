<?xml version="1.0" encoding="UTF-8"?>
<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.3 http://maven.apache.org/xsd/assembly-1.1.3.xsd">

	<id>bundle</id>
	<formats>
		<format>zip</format>
	</formats>
	<!-- No wrapping directory: the zip unpacks straight into an installation
	     directory, giving it the layout the readme and the jar expect
	     (aron2.jar next to config/). -->
	<includeBaseDirectory>false</includeBaseDirectory>

	<dependencySets>
		<dependencySet>
			<includes>
				<include>com.lightcomp:aron-distribution</include>
			</includes>
			<!-- aron-core and aron-ui are inside the fat jar already -->
			<useTransitiveDependencies>false</useTransitiveDependencies>
			<outputDirectory/>
			<!-- Unversioned on purpose: the version is in the zip name, while the
			     service unit and the run command refer to a stable file name. -->
			<outputFileNameMapping>aron2.jar</outputFileNameMapping>
		</dependencySet>
	</dependencySets>

	<fileSets>
		<fileSet>
			<directory>src/assembly/content</directory>
			<outputDirectory/>
		</fileSet>
		<!-- Taken from the distribution module rather than copied here, so the
		     template a developer copies into run/ and the one a deployment gets
		     can never drift apart. -->
		<fileSet>
			<directory>${project.basedir}/../distribution/config</directory>
			<outputDirectory>config</outputDirectory>
			<includes>
				<include>application.yml.template</include>
			</includes>
		</fileSet>
	</fileSets>

	<files>
		<file>
			<source>${project.basedir}/../LICENSE</source>
			<outputDirectory/>
		</file>
	</files>
</assembly>
